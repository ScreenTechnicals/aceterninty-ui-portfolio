import { AboutSection, HeroSection } from "@/components";

const Home = () => {
  return (
    <main className="min-h-svh relative bg-[#000319]">
      <HeroSection />
      <AboutSection />
    </main>
  );
};

export default Home;
